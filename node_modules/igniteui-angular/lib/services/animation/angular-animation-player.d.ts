import { AnimationPlayer as AngularAnimationPlayer } from '@angular/animations';
import { EventEmitter } from '@angular/core';
import { IBaseEventArgs } from '../../core/utils';
import { AnimationPlayer } from './animation';
export declare class IgxAngularAnimationPlayer implements AnimationPlayer {
    private internalPlayer;
    private _innerPlayer;
    animationStart: EventEmitter<IBaseEventArgs>;
    animationEnd: EventEmitter<IBaseEventArgs>;
    get position(): number;
    set position(value: number);
    constructor(internalPlayer: AngularAnimationPlayer);
    init(): void;
    play(): void;
    finish(): void;
    reset(): void;
    destroy(): void;
    hasStarted(): boolean;
    private onDone;
}
