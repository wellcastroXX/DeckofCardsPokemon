import { EventEmitter } from '@angular/core';
/** @hidden @internal */
export declare abstract class IgxPaginatorToken {
    abstract page: number;
    abstract perPage: number;
    abstract totalRecords: number;
    abstract pageChange: EventEmitter<number>;
    abstract paginate(val: number): void;
}
